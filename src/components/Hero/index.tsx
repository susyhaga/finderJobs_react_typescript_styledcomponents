import styles from './Hero.module.css'

const Hero = () => (
  <form className={styles.form}>
    <div className="container">
      <h2 className={styles.heroTitle}>
        Find the best developer jobs that match your skills.
      </h2>
    </div>
  </form>
)

export default Hero

